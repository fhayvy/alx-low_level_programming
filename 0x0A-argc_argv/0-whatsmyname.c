#include <stdio.h>

/**
 * main - Entry point
 * @argc: The size of array argv
 * @argv: An array containing program line arguments
 *
 * Return: 0 if Success
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
