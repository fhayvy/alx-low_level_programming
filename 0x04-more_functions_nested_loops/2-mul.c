#include "main.h"

/**
 * mul - Multiplies two integers
 *
 * @a: The first Integer
 * @b: The second Integer
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	int ch;

	ch = (a * b);

	return(ch);
}
