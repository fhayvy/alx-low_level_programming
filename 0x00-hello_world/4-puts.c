#include<stdio.h>

/*
 * main is the entry point
 * and it returns 0
 */
int main(void)
{
	char first_var[] = "\"Programming is like building a multilingual puzzle";

	puts(first_var);

	return (0);
}
