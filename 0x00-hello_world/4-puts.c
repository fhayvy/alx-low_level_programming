#include<stdio.h>

/*
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char first_var[] = "\"Programming is like building a multilingual puzzle";

	puts(first_var);

	return (0);
}
